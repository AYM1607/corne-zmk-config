to_wBAS_kp: behavior_to_kp {
    compatible = "zmk,behavior-hold-tap";
    label = "to_kp";
    #binding-cells = <2>;
    tapping-term-ms = <HP_TAPPING>;
    flavor = "hold-preferred";
    bindings = <&to_wBAS>, <&kp>;
};

MOTO_HOLD(w, AXN, BAS, FNK)
MOTO_HOLD(w, AXN, BAS, AXN)
MOTO_HOLD(w, DEV, BAS, DEV)
MOTO_HOLD(w, AXN, DEV, BAS)
MOTO_HOLD(w, AXN, DEV, AXN)
MOTO_HOLD(w, FNK, AXN, FNK)
